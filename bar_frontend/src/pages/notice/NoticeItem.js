import React from 'react'
import EditIcon from '../../assets/icons/EditIcon'
import { getBaseAPIRootUrl } from '../../utils/helper'

const NoticeItem = ({noticeItem, index, user}) => {
  return (
    <div></div>
  )
}

export default NoticeItem
